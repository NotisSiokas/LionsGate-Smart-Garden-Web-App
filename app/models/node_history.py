from app import db


class NodeHistory(db.Model):
    __tablename__ = 'node_history'

    id =
    FK1 =
    node_id =
    user_id =
    timestamp =
    event_type_id =
    description =

    def __repr__(self):
        return '{}'.format(self.name)